AGENTS_HOST=0.0.0.0
AGENTS_PORT=8090
OPENAI_API_KEY=
DEEP_AGENT_MODEL=openai:gpt-4o-mini
# Must match JWT_SECRET in backend/.env
JWT_SECRET=changeme
# Required — must match APP_DATABASE_URL in backend/.env
APP_DATABASE_URL=postgresql://user:pass@localhost:5432/startix
BOOTSTRAP_MAX_CONCURRENCY=3
BOOTSTRAP_INCLUDE_BULL_BEAR=true
